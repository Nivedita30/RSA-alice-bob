import scapy
from scapy.all import *
# from scapy import IP
# Create an IP header
# ip = IP(dst="8.8.8.8")

# # Create an ICMP echo request packet
# icmp = ICMP()

# # Combine the IP header and ICMP packet to create the final packet
# packet = ip/icmp

# # Send the packet and receive the response
# reply = sr1(packet, timeout=2)

# # Print the response
# if reply is None:
#     print("No response received")
# else:
#     print(reply.summary())
